# Tasks: api-crud-subjects

- [ ] Corrigir GET /subjects para filtrar por user_id
- [ ] Corrigir POST /subjects para salvar user_id do usuário autenticado
- [ ] Corrigir PATCH /subjects/{id} para filtrar por user_id
- [ ] Corrigir DELETE /subjects/{id} para filtrar por user_id
