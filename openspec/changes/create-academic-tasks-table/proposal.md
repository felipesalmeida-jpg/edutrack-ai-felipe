# Change: create-academic-tasks-table

## Why
Os alunos precisam registrar e gerenciar suas obrigações acadêmicas (lições, 
provas, trabalhos) vinculadas a cada disciplina.

## What Changes
- Criação da tabela `academic_tasks` no Xano com os campos: `title`, 
`description`, `due_date`, `status` e `subject_id`

## Impact
- Backend: nova tabela no Xano
- Sem impacto no frontend neste momento